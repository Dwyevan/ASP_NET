using Microsoft.AspNetCore.SignalR;

namespace CMS.BA.Hubs
{
    public class AdminHub : Hub
    {
        // Hub này được sử dụng để đẩy thông báo thời gian thực đến Admin (danh sách khách hàng mới, đơn hàng mới, v.v.)
    }
}
